import StudentLayout from '@/layouts/StudentLayout';
import React from 'react';

const OrderPage = () => {
    return (
        <StudentLayout>
        <h1>Order history .. </h1>
      </StudentLayout>
    );
};

export default OrderPage;